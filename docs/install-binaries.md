# Install binaries

To help local contract development it's recommended to install your freshly compiled binaries into `/usr/local` using the `eosio_install.sh` script.

```bash
cd ~/proton/proton
sudo ./scripts/eosio_install.sh
```